<% if resource.errors.any? %>
  <div class="text-center alert alert-danger mx-auto p-4 alert-dismissable fade show" role="alert" style="width: 60vw;" >
    
    <button type="button" class="close" data-dismiss="alert" aria-label="Close">
      <span aria-hidden="true">×</span>
    </button>

    <h2>Nous n'avons pu effectuer cette action car :</h2>
    <ul>
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
